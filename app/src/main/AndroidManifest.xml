<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.sandeep.tmdbclentandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".presentation.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TMDBClentAndroid">
        <activity
            android:name=".presentation.tvShows.TvShowsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.artists.ArtistsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.movie.MovieActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>